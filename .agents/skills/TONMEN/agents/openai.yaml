interface:
  display_name: "TONMEN"
  short_description: "Repo-specific patterns and workflows for TONMEN"
  default_prompt: "Use the TONMEN repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true